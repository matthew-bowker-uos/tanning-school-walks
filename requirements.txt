# Runtime requirements for schools-sunbeds.
#
# Single source of truth for both local dev and Colab. Install with:
#   pip install -r requirements.txt
#   pip install -e .            # makes `import schools_sunbeds` work
#
# For development tooling (pytest, ruff, black, pre-commit), additionally:
#   pip install -r requirements-dev.txt

# Spatial core
geopandas>=1.0
shapely>=2.0
pyproj>=3.6
fiona>=1.10
pyogrio>=0.9
rtree>=1.3

# Routing / network
pandana>=0.7
osmnx>=2.0
networkx>=3.3
pyrosm>=0.6.2

# Tabular / numerical
pandas>=2.2
numpy>=1.26,<2.1
pyarrow>=17.0

# HTTP / APIs
requests>=2.32
googlemaps>=4.10
tenacity>=9.0

# Stats
statsmodels>=0.14
linearmodels>=6.0
scipy>=1.13

# Matching
rapidfuzz>=3.9

# Visualisation
matplotlib>=3.9
contextily>=1.6
mapclassify>=2.8

# Notebook & utilities
papermill>=2.6
python-dotenv>=1.0
pyyaml>=6.0
tqdm>=4.66
